# AGENTS.md

## Scope

Hyrox ticket-monitor application.

## Initial read set

Read only `README.md` and the exact file named by the task. Do not recursively inspect the repository or open unrelated application, state, workflow, or configuration files.

## Routing

For monitor behavior, inspect `monitor.py`. For the web interface, inspect `index.html` and `app.js`. For persisted monitor state, inspect only the specific file under `state/` named by the task.

## Work protocol

State the exact files to inspect before editing. Make one bounded change, run the narrowest relevant check, and report changed files and verification. Preserve existing ticket-monitor state and avoid changing deployment workflows or generated state unless requested.
